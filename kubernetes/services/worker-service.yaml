apiVersion: v1
kind: Service
metadata:
  name: worker-a-service
  namespace: platform
spec:
  selector:
    app: worker-a
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
  type: ClusterIP
---
apiVersion: v1
kind: Service
metadata:
  name: worker-b-service
  namespace: platform
spec:
  selector:
    app: worker-b
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
  type: ClusterIP
---
apiVersion: v1
kind: Service
metadata:
  name: worker-c-service
  namespace: platform
spec:
  selector:
    app: worker-c
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
  type: ClusterIP
